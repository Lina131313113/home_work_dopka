# 🛒 Корзина товаров с иерархией классов

Проект реализует иерархию классов для товаров (продуктов) с возможностью их добавления в корзину, подсчёта стоимости с учётом скидок и фильтрации по вегетарианским продуктам.

## 🗂 Структура проекта

src/    
│   
├── 🧱 model/ # Пакет с моделями товаров     
│ │     
│ ├── 🧾 Food.java # Абстрактный класс "Продукт"     
│ ├── 🥩 Meat.java # Класс "Мясо"        
│ ├── 🍎 Apple.java # Класс "Яблоко"     
│ ├── 🏷️ Discountable.java # Интерфейс "Скидочный"      
│ │     
│ └── 🔧 constants/ # Подпакет с константами     
│ ├── 💰 Discount.java # Константы скидок        
│ └── 🎨 Colour.java # Константы цветов      
│       
├── 🛒 service/ # Пакет сервисов     
│ └── 🧺 ShoppingCart.java # Класс "Корзина покупок"     
│       
└── ▶️ Main.java # Главный класс с точкой входа     

## 📦 Описание классов

### 🧾 `Food` (абстрактный класс)
Базовый класс для всех продуктов. Содержит:
- `amount` — количество в кг (int)
- `price` — цена за кг (double)
- `isVegetarian` — флаг вегетарианства (boolean)

Поля защищены (`protected`), доступны только наследникам.

---

### 📉 `Discountable` (интерфейс)
Определяет метод:
- `double getDiscount()` — возвращает размер скидки в процентах.

По умолчанию все реализации возвращают `0`, если не указано иное.

---

### 🥩 `Meat` (класс)
Наследник `Food`, реализует `Discountable`.
- Конструктор: `Meat(int amount, double price)`
- Автоматически `isVegetarian = false`
- Скидка всегда `0`

---

### 🍎 `Apple` (класс)
Наследник `Food`, реализует `Discountable`.
- Дополнительное поле: `colour` (String)
- Конструктор: `Apple(int amount, double price, String colour)`
- Автоматически `isVegetarian = true`
- Скидка: `60%` если `colour = "red"`, иначе `0`

---

### 🏷 `model.constants.Discount`
Содержит константу:
- `RED_APPLE = 60.0`

---

### 🎨 `model.constants.Colour`
Содержит строковые константы цветов:
- `RED = "red"`
- `GREEN = "green"`

---

### 🛒 `service.ShoppingCart`
Класс для работы с корзиной товаров.
- Поле: `Food[] items`
- Конструктор: `ShoppingCart(Food[])`
- Методы:
    - `getTotalPriceWithoutDiscount()` — общая сумма без скидок
    - `getTotalPriceWithDiscount()` — общая сумма со скидками
    - `getVegetarianTotalPrice()` — сумма вегетарианских товаров без скидок

---

### ▶️ `Main`
Точка входа в программу.
- Создаёт объекты: мясо, красные и зелёные яблоки
- Помещает их в корзину
- Выводит на экран:
    1. Общую сумму без скидки
    2. Общую сумму со скидкой
    3. Сумму вегетарианских продуктов без скидки

## 🚀 Запуск

1. Склонируйте репозиторий
2. Скомпилируйте проект:
   ```bash
   javac -d out src/**/*.java src/Main.java